fix: semEtapaCount try/catch, remove realizedVal modal, cards sempre visíveis, botão salvar %

FIX 1 - CRITICO: semEtapaCount derrubava summary endpoint
  - financial.routes.ts: envolver count em try/catch (fallback = 0)
  - Corrigir filtro: companyId via transaction (CostCenterAllocation nao tem companyId direto)
  - Sem try/catch a query lancando erro retornava HTTP 500
    frontend recebia r.ok=false, allocSummary ficava null, cards mostravam --

FIX 2 - Modal de progresso: remover campo "Valor realizado"
  - Remover estado realizedVal, setRealizedVal
  - handleProgressSave: remover realizedValue do body do PATCH
  - Renomear savingProgress (boolean) para savingProgressModal (evitar colisao com BudgetTable)
  - Modal agora mostra apenas: % Executado + info de realizado atual (somente leitura)
  - Label atualizado: "% Executado (físico)"

FIX 3 - Cards Material+MO sempre visiveis
  - Remover condicional (project?.materialCosts || project?.laborCosts)
  - Sempre renderizar os 2 cards com valor 0 como fallback

FIX 4 - Botao salvar explicito no BudgetTable (substituir onBlur)
  - editingProgress: Record<stageId, number> -- valor sendo editado
  - Input altera editingProgress localmente (sem salvar ainda)
  - Botao "Salvar" aparece apenas quando valor mudou vs stage.progressPercent
  - Clicar em salvar: PATCH + limpa editingProgress[stageId]
  - Sem botao: barra de progresso azul visivel

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
