fix: etapas — % executado fisico manual + valor realizado automático das apropriações

Backend:
- GET /:id: progressPercent usa valor fisico do DB (nao sobrescreve com razão financeira)
- updateProgressSchema: remove realizedValue (não é editável pelo usuário)
- PATCH /stages/:stageId/progress: remove data.realizedValue
- recalcProject: calcula realizedValue dinamicamente (allocs + entries por stageId)
  para corrigir budgetAlert do projeto (antes usava stale DB realizedValue)

Frontend BudgetTable:
- Coluna Progresso: input inline para % físico editável
  - onChange atualiza estado local (localProgress)
  - onBlur salva via PATCH /stages/:stageId/progress
  - Barra azul mostra % físico digitado em tempo real
  - Spinner enquanto salva
- avgProgress: média ponderada pelo orçado (antes era média simples)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
